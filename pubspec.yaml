name: profil_app
description: Une application Flutter simple affichant une carte de profil utilisateur.
publish_to: "none" # On ne publie pas cette app sur pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Ajoute ici d'autres dépendances si besoin plus tard
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/profile.jpg
